<form method="post" action="{{ path('admin_point_delete', {'id': point.id}) }}" onsubmit="return confirm('êtes vous sure de vouloir supprimer ce point ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ point.id) }}">
    <button class="btn bg-danger text-white">Supprimer</button>
</form>
