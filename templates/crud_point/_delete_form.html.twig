<form method="post" action="{{ path('crud_point_delete', {'id': point.id}) }}" onsubmit="return confirm('voulez vous vraiment supprimer ce point ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ point.id) }}">
    <button class="btn bg-danger text-white">Supprimer</button>
</form>
